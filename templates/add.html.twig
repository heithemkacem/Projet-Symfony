{% extends 'base.html.twig' %}

{% block title %}Add a New Plat{% endblock %}

{% block body %}
    <h1>Add a New Plat</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Create</button>
    {{ form_end(form) }}
{% endblock %}
