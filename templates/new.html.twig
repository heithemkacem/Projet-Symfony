{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create a new regime</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('regime_index') }}">Back to the list</a>
{% endblock %}
